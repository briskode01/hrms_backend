# Node modules (will be installed inside container)
node_modules

# Logs
npm-debug.log
yarn-debug.log
yarn-error.log

# Environment files (never include secrets in image)
.env
.env.*

# Git
.git
.gitignore

# Docker files
Dockerfile
.dockerignore

# OS / system files
.DS_Store
Thumbs.db

# Optional: tests & docs (if not needed in production)
tests
coverage
docs

# Optional: local config files
*.local